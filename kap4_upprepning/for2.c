#include <stdio.h>
int main() {
	
	// Beräknar värdet av x^n

	double res;
	int i;
	for (res = 1, i = 1; i <= n; i = i + 1)	// OBS initieringsdelen innehåller TVÅ tilldelningar!
		res = res * x
	
	printf("%d", res);

	return 0;
}

// Init; villkor; ändring kan uteslutas, men semikolon måste vara kvar.